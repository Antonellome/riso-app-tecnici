rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read; // CHIUNQUE può leggere
      allow write: if request.auth != null; // SOLO gli utenti loggati possono scrivere
    }
  }
}
